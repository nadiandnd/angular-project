import { HighlightBoldPipe } from './highlight-bold.pipe';

describe('HighlightBoldPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightBoldPipe();
    expect(pipe).toBeTruthy();
  });
});
